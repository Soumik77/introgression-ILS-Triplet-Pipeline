# Data directories

- `manifests/`: deterministic simulation grids generated from YAML configs.
- `gene_trees/`: one multi-Newick `.tre` file per simulation.
- `features/`: one CSV row per simulation and taxon triplet.

Large generated data are intentionally not bundled with the source package.

